server:
  port: 18032
spring:
  application:
    name: server-turbine
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    # 务必与server.port一致
    non-secure-port: 18032
management:
  # 配置spring boot actuator端口
  port: 8032
